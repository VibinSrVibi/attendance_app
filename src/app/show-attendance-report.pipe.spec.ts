import { ShowAttendanceReportPipe } from './show-attendance-report.pipe';

describe('ShowAttendanceReportPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowAttendanceReportPipe();
    expect(pipe).toBeTruthy();
  });
});
